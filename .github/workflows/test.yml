name: Deploy the redis to the server
on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: "k23UKHKQvr11CNTRL"
          oauth-secret: "tskey-client-k23UKHKQvr11CNTRL-VaZhVZB9i2hXCby6Boud2hAZzJ5PQH1Fb"
          tags: "tag:ci"

      - run: ip ad sh
